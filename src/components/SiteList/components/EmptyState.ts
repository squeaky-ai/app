import styled from 'styled-components';

const EmptyState = styled.div`
  margin: 0 auto;
  max-width: 560px;
  text-align: center;

  h2 {
    font-size: 48px;
    font-weight: 800;
    line-height: 64px;
  }

  button {
    margin: 5rem 0;
  }
`;

export default EmptyState;
