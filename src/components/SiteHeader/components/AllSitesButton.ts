import styled from 'styled-components';

const AllSitesButton = styled.div`
  a {
    color: ${({ theme }) => theme.colors.default.neutralMedium};
    font-size: 13px; // TODO
  }
`;

export default AllSitesButton;
