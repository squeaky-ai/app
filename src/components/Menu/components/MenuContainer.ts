import styled from 'styled-components';

const MenuContainer = styled.div`
  position: relative;

  & [data-dropdown-icon] {
    height: 2.4rem;
    width: 2.4rem;
  }
`;

export default MenuContainer;
