#!/bin/sh

npx apollo service:download --endpoint=http://localhost:4000/api/graphql graphql-schema.json

npx apollo codegen:generate --localSchemaFile=graphql-schema.json --target=typescript --tagName=gql --outputFlat ./src/types/graphql.ts
